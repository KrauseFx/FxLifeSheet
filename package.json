{
    "scripts": {
        "prettier": "prettier worker.ts web.ts classes/*.ts --write",
        "typescript": "tsc --p tsconfig.json",
        "worker": "source .keys && npm run prettier && npm run typescript && node worker.js",
        "web": "source .keys && npm run prettier && npm run typescript && node web.js",
        "scheduler": "source .keys && npm run prettier && npm run typescript && node scheduler.js"
    },
    "dependencies": {
        "moment": "^2.24.0",
        "needle": "^2.3.3",
        "node-cron": "^3.0.2",
        "pg": "^8.7.1",
        "prettier": "^1.19.1",
        "telegraf": "^3.36.0",
        "typescript": "^3.8.3"
    },
    "devDependencies": {
        "@types/node": "~16.0.0"
    }
}